# 루트 프로젝트 작업 지침

## 문서 작성 순서

루트 프로젝트에서 작업을 진행할 때는 **항상 문서를 먼저 작성**한다. 구현보다 문서가 선행되며, 다음 순서를 반드시 지킨다.

```
요구사항 문서  →  컨텍스트 문서  →  스펙 문서  →  구현
```

- 앞 단계 문서가 합의되기 전에는 다음 단계 문서로 넘어가지 않는다.
- 스펙 문서가 합의된 뒤에 구현에 들어간다.
- 세 문서는 **주제별로 파일을 분리**한다. 하나의 파일에 여러 주제를 섞지 않는다.

### 위치와 구성

- 요구사항 문서: `docs/requirement/` (예: `docs/requirement/camera-capture.md`)
- 컨텍스트 문서: `docs/context/` (예: `docs/context/camera-capture.md`)
- 스펙 문서: `docs/spec/` (예: `docs/spec/camera-capture.md`)
- 같은 주제는 세 디렉터리에서 **동일한 파일 이름**을 사용해 서로 대응되도록 한다.

---

## 1. 요구사항 문서

실제 앱 기획자가 원하는 바를 기술하는 문서다.

- **코드 레벨 설명을 추가하지 않는다.** (클래스/함수 시그니처, 코드 스니펫, 구현 세부 등은 제외)
- **플랫폼 특징을 구분하지 않는다.** Android / iOS 섹션을 만들지 않고, 플랫폼 무관하게 무엇을(What) 원하는지만 기술한다.
- 사용자가 기대하는 동작과 결과를 기준으로 작성한다.

## 2. 컨텍스트 문서

개발자 관점에서 요구사항을 각 플랫폼에서 실제로 구현 가능한지 정리하는 문서다.

- 요구사항의 각 항목이 **Android / iOS 에서 구현 가능한지** 정리한다.
- 구현이 **불가능하거나 제약이 있으면 그 이유도 함께** 정리한다.
- 추측으로 적지 않는다. 실제 **Android Developer 공식 문서, Apple 공식 문서**를 탐색해 사실을 확인한 뒤 작성한다.
- 참고한 문서의 **링크를 함께 기록**한다. (어떤 내용에 대한 근거인지 알 수 있게 항목과 연결)

## 3. 스펙 문서

요구사항 문서와 컨텍스트 문서를 절충해, 실제로 구현 가능한 스펙을 확정하는 문서다.

- 요구사항(원하는 것)과 컨텍스트(가능 여부·제약)를 종합해 **구현 가능한 동작**으로 정리한다.
- **코드 레벨 설명을 추가하지 않는다.** 무엇을(What) / 어떻게 동작하는지(Behavior)를 기술하고, 어떻게 구현하는지(How, 코드)는 다루지 않는다.

### 플랫폼 공통 / 플랫폼별 스펙 구분

- 기본은 **플랫폼 공통 스펙**으로 작성한다.
- 플랫폼 차이로 공통 구현이 **불가능한 경우에만** Android / iOS 를 구분하여 작성한다. 이때 다음을 명시한다.
  - 각 플랫폼별로 어떻게 **다른 스펙**(동작)을 갖는지
  - **왜 다르게 구현**되어야 하는지 (어떤 플랫폼 제약 때문에 공통 구현이 불가능한지 — 컨텍스트 문서의 근거와 연결)
- 플랫폼 구분이 필요 없는 동작은 공통 스펙에만 기술하고 Android / iOS 섹션을 만들지 않는다.
