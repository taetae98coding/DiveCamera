# Feature Entry Provider API TC

## unitTest

- 해당 없음. feature entry provider API는 Navigation3 DSL과 모듈 공개 API 경계에 대한 계약이며, 현재 JVM 순수 로직 API로 분리되어 있지 않다.

## uiTest

- 해당 없음. Screen 렌더링은 각 feature 화면 TC에서 검증하고, feature entry provider 조합은 앱 모듈 컴파일로 검증한다.
